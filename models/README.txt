spawn_classifier.pt (not present yet): a torch.load-able callable (e.g. a scripted/pickled nn.Module) taking a float32 tensor of shape (1, 4, 128, 128) — the SAM-masked crop with channels [R, G, B, NIR] scaled 0-1, pixels outside the mask zeroed — and returning a single spawn logit (prob = sigmoid(logit)). Until this file exists, app/segment.py scores segments with its built-in spectral heuristic.
